/*Copyright (c) 2016-2017 performance-contractors.com All Rights Reserved.
 This software is the confidential and proprietary information of performance-contractors.com You shall not disclose such Confidential Information and shall use it only in accordance
 with the terms of the source code license agreement you entered into with performance-contractors.com*/
package com.psabackoffice.psa.service;

/*This is a Studio Managed File. DO NOT EDIT THIS FILE. Your changes may be reverted by Studio.*/

import java.util.Map;

import javax.validation.Valid;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.wavemaker.runtime.data.exception.EntityNotFoundException;
import com.wavemaker.runtime.data.export.ExportType;
import com.wavemaker.runtime.data.expression.QueryFilter;
import com.wavemaker.runtime.data.model.AggregationInfo;
import com.wavemaker.runtime.file.model.Downloadable;

import com.psabackoffice.psa.RefWeather;

/**
 * Service object for domain model class {@link RefWeather}.
 */
public interface RefWeatherService {

    /**
     * Creates a new RefWeather. It does cascade insert for all the children in a single transaction.
     *
     * This method overrides the input field values using Server side or database managed properties defined on RefWeather if any.
     *
     * @param refWeather Details of the RefWeather to be created; value cannot be null.
     * @return The newly created RefWeather.
     */
	RefWeather create(@Valid RefWeather refWeather);


	/**
	 * Returns RefWeather by given id if exists.
	 *
	 * @param refweatherId The id of the RefWeather to get; value cannot be null.
	 * @return RefWeather associated with the given refweatherId.
     * @throws EntityNotFoundException If no RefWeather is found.
	 */
	RefWeather getById(Integer refweatherId) throws EntityNotFoundException;

    /**
	 * Find and return the RefWeather by given id if exists, returns null otherwise.
	 *
	 * @param refweatherId The id of the RefWeather to get; value cannot be null.
	 * @return RefWeather associated with the given refweatherId.
	 */
	RefWeather findById(Integer refweatherId);


	/**
	 * Updates the details of an existing RefWeather. It replaces all fields of the existing RefWeather with the given refWeather.
	 *
     * This method overrides the input field values using Server side or database managed properties defined on RefWeather if any.
     *
	 * @param refWeather The details of the RefWeather to be updated; value cannot be null.
	 * @return The updated RefWeather.
	 * @throws EntityNotFoundException if no RefWeather is found with given input.
	 */
	RefWeather update(@Valid RefWeather refWeather) throws EntityNotFoundException;

    /**
	 * Deletes an existing RefWeather with the given id.
	 *
	 * @param refweatherId The id of the RefWeather to be deleted; value cannot be null.
	 * @return The deleted RefWeather.
	 * @throws EntityNotFoundException if no RefWeather found with the given id.
	 */
	RefWeather delete(Integer refweatherId) throws EntityNotFoundException;

	/**
	 * Find all RefWeathers matching the given QueryFilter(s).
     * All the QueryFilter(s) are ANDed to filter the results.
     * This method returns Paginated results.
	 *
     * @deprecated Use {@link #findAll(String, Pageable)} instead.
	 *
     * @param queryFilters Array of queryFilters to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching RefWeathers.
     *
     * @see QueryFilter
     * @see Pageable
     * @see Page
	 */
    @Deprecated
	Page<RefWeather> findAll(QueryFilter[] queryFilters, Pageable pageable);

    /**
	 * Find all RefWeathers matching the given input query. This method returns Paginated results.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching RefWeathers.
     *
     * @see Pageable
     * @see Page
	 */
    Page<RefWeather> findAll(String query, Pageable pageable);

    /**
	 * Exports all RefWeathers matching the given input query to the given exportType format.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param exportType The format in which to export the data; value cannot be null.
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
     * @return The Downloadable file in given export type.
     *
     * @see Pageable
     * @see ExportType
     * @see Downloadable
	 */
    Downloadable export(ExportType exportType, String query, Pageable pageable);

	/**
	 * Retrieve the count of the RefWeathers in the repository with matching query.
     * Note: Go through the documentation for <u>query</u> syntax.
     *
     * @param query query to filter results. No filters applied if the input is null/empty.
	 * @return The count of the RefWeather.
	 */
	long count(String query);

	/**
	 * Retrieve aggregated values with matching aggregation info.
     *
     * @param aggregationInfo info related to aggregations.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
	 * @return Paginated data with included fields.

     * @see AggregationInfo
     * @see Pageable
     * @see Page
	 */
	Page<Map<String, Object>> getAggregatedValues(AggregationInfo aggregationInfo, Pageable pageable);


}

