/*Copyright (c) 2016-2017 performance-contractors.com All Rights Reserved.
 This software is the confidential and proprietary information of performance-contractors.com You shall not disclose such Confidential Information and shall use it only in accordance
 with the terms of the source code license agreement you entered into with performance-contractors.com*/
package com.psabackoffice.psa.service;

/*This is a Studio Managed File. DO NOT EDIT THIS FILE. Your changes may be reverted by Studio.*/

import java.util.Map;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import com.wavemaker.runtime.data.exception.EntityNotFoundException;
import com.wavemaker.runtime.data.export.ExportType;
import com.wavemaker.runtime.data.expression.QueryFilter;
import com.wavemaker.runtime.data.model.AggregationInfo;
import com.wavemaker.runtime.file.model.Downloadable;

import com.psabackoffice.psa.FeedBack;
import com.psabackoffice.psa.FeedBackNotes;

/**
 * Service object for domain model class {@link FeedBack}.
 */
public interface FeedBackService {

    /**
     * Creates a new FeedBack. It does cascade insert for all the children in a single transaction.
     *
     * This method overrides the input field values using Server side or database managed properties defined on FeedBack if any.
     *
     * @param feedBack Details of the FeedBack to be created; value cannot be null.
     * @return The newly created FeedBack.
     */
	FeedBack create(FeedBack feedBack);


	/**
	 * Returns FeedBack by given id if exists.
	 *
	 * @param feedbackId The id of the FeedBack to get; value cannot be null.
	 * @return FeedBack associated with the given feedbackId.
     * @throws EntityNotFoundException If no FeedBack is found.
	 */
	FeedBack getById(Integer feedbackId) throws EntityNotFoundException;

    /**
	 * Find and return the FeedBack by given id if exists, returns null otherwise.
	 *
	 * @param feedbackId The id of the FeedBack to get; value cannot be null.
	 * @return FeedBack associated with the given feedbackId.
	 */
	FeedBack findById(Integer feedbackId);


	/**
	 * Updates the details of an existing FeedBack. It replaces all fields of the existing FeedBack with the given feedBack.
	 *
     * This method overrides the input field values using Server side or database managed properties defined on FeedBack if any.
     *
	 * @param feedBack The details of the FeedBack to be updated; value cannot be null.
	 * @return The updated FeedBack.
	 * @throws EntityNotFoundException if no FeedBack is found with given input.
	 */
	FeedBack update(FeedBack feedBack) throws EntityNotFoundException;

    /**
	 * Deletes an existing FeedBack with the given id.
	 *
	 * @param feedbackId The id of the FeedBack to be deleted; value cannot be null.
	 * @return The deleted FeedBack.
	 * @throws EntityNotFoundException if no FeedBack found with the given id.
	 */
	FeedBack delete(Integer feedbackId) throws EntityNotFoundException;

	/**
	 * Find all FeedBacks matching the given QueryFilter(s).
     * All the QueryFilter(s) are ANDed to filter the results.
     * This method returns Paginated results.
	 *
     * @deprecated Use {@link #findAll(String, Pageable)} instead.
	 *
     * @param queryFilters Array of queryFilters to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching FeedBacks.
     *
     * @see QueryFilter
     * @see Pageable
     * @see Page
	 */
    @Deprecated
	Page<FeedBack> findAll(QueryFilter[] queryFilters, Pageable pageable);

    /**
	 * Find all FeedBacks matching the given input query. This method returns Paginated results.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of matching FeedBacks.
     *
     * @see Pageable
     * @see Page
	 */
    Page<FeedBack> findAll(String query, Pageable pageable);

    /**
	 * Exports all FeedBacks matching the given input query to the given exportType format.
     * Note: Go through the documentation for <u>query</u> syntax.
	 *
     * @param exportType The format in which to export the data; value cannot be null.
     * @param query The query to filter the results; No filters applied if the input is null/empty.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
     * @return The Downloadable file in given export type.
     *
     * @see Pageable
     * @see ExportType
     * @see Downloadable
	 */
    Downloadable export(ExportType exportType, String query, Pageable pageable);

	/**
	 * Retrieve the count of the FeedBacks in the repository with matching query.
     * Note: Go through the documentation for <u>query</u> syntax.
     *
     * @param query query to filter results. No filters applied if the input is null/empty.
	 * @return The count of the FeedBack.
	 */
	long count(String query);

	/**
	 * Retrieve aggregated values with matching aggregation info.
     *
     * @param aggregationInfo info related to aggregations.
     * @param pageable Details of the pagination information along with the sorting options. If null exports all matching records.
	 * @return Paginated data with included fields.

     * @see AggregationInfo
     * @see Pageable
     * @see Page
	 */
	Page<Map<String, Object>> getAggregatedValues(AggregationInfo aggregationInfo, Pageable pageable);

    /*
     * Returns the associated feedBackNoteses for given FeedBack id.
     *
     * @param id value of id; value cannot be null
     * @param pageable Details of the pagination information along with the sorting options. If null returns all matching records.
     * @return Paginated list of associated FeedBackNotes instances.
     *
     * @see Pageable
     * @see Page
     */
    Page<FeedBackNotes> findAssociatedFeedBackNoteses(Integer id, Pageable pageable);

}

