/**This is a Studio Managed File. DO NOT EDIT THIS FILE. Your changes may be reverted by Studio.*/
package com.psabackoffice.email.controller;

import com.psabackoffice.email.Email;
import java.lang.String;
import org.springframework.web.bind.annotation.*;
import org.springframework.http.MediaType;
import org.springframework.beans.factory.annotation.Autowired;
import com.wordnik.swagger.annotations.Api;
import com.wordnik.swagger.annotations.ApiOperation;
import com.wordnik.swagger.annotations.ApiParam;
import com.wavemaker.tools.api.core.annotations.WMAccessVisibility;
import com.wavemaker.tools.api.core.models.AccessSpecifier;

@RestController
@RequestMapping(value = "/email")
public class EmailController {

    @Autowired
    private Email email;

    @RequestMapping(value = "/sendEmail", produces = "application/json", method = RequestMethod.GET)
    public String sendEmail(@RequestParam(value = "from", required = false) String from, @RequestParam(value = "subject", required = false) String subject, @RequestParam(value = "body", required = false) String body, @RequestParam(value = "fileDir", required = false) String fileDir, @RequestParam(value = "fileName", required = false) String fileName, @RequestParam(value = "to", required = false) String to, @RequestParam(value = "emailServer", required = false) String emailServer, @RequestParam(value = "emailPort", required = false) String emailPort, @RequestParam(value = "userName", required = false) String userName, @RequestParam(value = "password", required = false) String password) {
        return email.sendEmail(from, subject, body, fileDir, fileName, to, emailServer, emailPort, userName, password);
    }
}
